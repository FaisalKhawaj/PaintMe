import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  iconButton: {
    flex: 1,
    borderRadius: 12,
    paddingVertical: 13,
    height: 52,
    alignItems: "center",
    backgroundColor: "#EDEBEE",
  },
});
